# 3号工作报告：行情对比页面与接口接入

> 日期：2026-07-21  
> 分支：`web-foundation-market`  
> 代码范围：行情后端接口、`/market` 前端页面、fixture E2E 演示链路

## 已完成

- 已拉取并合并最新 `origin/main`，当前三号工作包含二号前端基础和三号行情后端能力。
- 后端行情接口保持冻结契约：`POST /api/market/compare`，请求使用 `symbols` + `range`，响应字段使用 `data.normalized_series`、`data.interval_returns`、`data.snapshots`、`data.source_details`。
- 完成 `/market` 行情对比页：
  - 白名单 ETF 多选：`510300`、`511010`、`518880`、`511880`；
  - 区间切换：`1M`、`3M`、`1Y`；
  - ECharts 归一化走势；
  - 最新快照、20d/60d/252d 区间收益表；
  - 数据来源核验表；
  - loading、empty、API error、fixture/fallback、warning、success 状态。
- 新增前端 fixture 契约 `frontend/mock/market-compare.json`，并接入 `frontend/e2e/mock-api.mjs` 的 `POST /api/market/compare`。
- 新增 `frontend/e2e/market.spec.ts`，覆盖行情页成功渲染和未选择标的校验。
- 修复 Windows 下 Playwright 内置 webServer teardown 偶发超时：`npm run e2e` 改为 `frontend/e2e/run-e2e.mjs` 统一启动并清理 mock API 与 Vite。

## 本次修改文件

- `frontend/src/features/market/MarketPage.tsx`
- `frontend/src/features/market/types.ts`
- `frontend/mock/market-compare.json`
- `frontend/e2e/market.spec.ts`
- `frontend/e2e/run-e2e.mjs`
- `frontend/e2e/mock-api.mjs`
- `frontend/e2e/ui-shell.spec.ts`
- `frontend/src/App.tsx`
- `frontend/src/api/keys.ts`
- `frontend/package.json`
- `frontend/playwright.config.ts`

## 验证结果

```text
npm ci                                      通过
npm run lint                               通过
npm run typecheck                          通过
npm run test                               12/12 通过
npm run build                              通过（仍有已知大 chunk 提示）
npm run e2e                                12/12 通过
ruff format --check .                      通过
ruff check .                               通过
mypy src                                   通过
pytest --cov=finance_advisor --cov-fail-under=85  48/48 通过，覆盖率 85.23%
scripts/verify-hermes.ps1                  通过
scripts/preflight.ps1                      通过，模型联通因未配置密钥按预期跳过
```

说明：PowerShell 脚本验证时当前沙箱用户与宿主用户不同，Git 触发 dubious ownership 保护；验证使用了本进程临时 `safe.directory` 环境变量，没有修改全局 Git 配置。

## 页面演示

```powershell
cd D:\financial-advisor-agent\repo\frontend
npm run e2e
npm run dev -- --host 127.0.0.1
```

前端地址：`http://127.0.0.1:5173/market`  
fixture API 地址：`http://127.0.0.1:8123`

已生成演示截图：

- `D:\financial-advisor-agent\repo\.runtime\market-1366x768.png`
- `D:\financial-advisor-agent\repo\.runtime\market-1920x1080.png`

## 待其他成员完成或合并

- 4号：风险模块分支 `origin/feat/risk-lab-mcp` 尚未进入 `main`；当前 `/risk` 前端仍是占位，后端 `src/finance_advisor/web/routes/risk.py` 仍是空 router。
- 5号：配置页 `/portfolio` 仍是占位，后端 `src/finance_advisor/web/routes/portfolio.py` 仍是空 router；真实配置规划页面和接口还需接入。
- 5号：`/advisor` 前端基础页已存在，但后端 `src/finance_advisor/web/routes/advisor.py` 仍是空 router；真实 Hermes CLI Adapter / Advisor API 还需完成。
- 2号或最终集成人：如需严格遵守所有权规则，可复核本次对 `App.tsx`、`api/keys.ts`、`mock-api.mjs`、`playwright.config.ts` 的最小共享接线；这些改动是为了让三号页面可以独立演示和通过 E2E。

## 风险提示

- 当前 fixture 明确标注“演示数据/非实时数据”，不得当作真实行情引用。
- 前端不计算或补造行情数字，页面展示值均来自 `POST /api/market/compare` 响应。
- 页面没有交易按钮、买入价、卖出价、收益承诺或预测文案。
